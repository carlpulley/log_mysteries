// INPUT: data = [ { 'label': <timeline label>, 'timeline': [ { 'begin': <event start (double expressing seconds)>, 'end': <event end (double expressing microseconds)> }, ... ] }]

var pb = 4, pt = 6, p = pb+pt, h = 3, c = p+h;
var width = 1100;
var height = data.length * c + 10;

var begin = function(ds) { return ds.timeline.map(function (d) { return d.begin; }).min(); }
var end = function(ds) { return ds.timeline.map(function (d) { return d.end; }).max(); }
var dates = pv.Scale.linear(data.map(function(ds) { return begin(ds); }).min(), data.map(function(ds) { return end(ds); }).max()).range(0, width);

var vis = new pv.Panel()
    .def("i", 0)
    .width(width)
    .height(height)
    .margin(20)
    .left(50)
    .right(400)
    .events("all")
    .event("mousemove", pv.Behavior.point(Infinity).collapse("y"));

vis.add(pv.Rule)
    .data(dates.ticks())
    .strokeStyle("#eee")
    .left(dates)
    .anchor("bottom").add(pv.Label)
        .text(function(d) pv.Format.date("%d/%b/%y %H:%M:%S")(new Date(d*1000)));
            
data.forEach(function(ds) {
    var line = vis.add(pv.Bar)
        .data(ds.timeline)
        .left(dates(begin(ds)))
        .width(dates(end(ds)) - dates(begin(ds)))
        .top(function() data.indexOf(ds) * c + p - 2)
        .height(h)
        .strokeStyle("#c4d8ff")
        .event("point", function() this.parent.i(this.parent.mouse().x));

    ds.timeline.forEach(function(d) {
        var line = vis.add(pv.Bar)
            .left(dates(d.begin))
            .width(dates(d.end) - dates(d.begin))
            .top(function() data.indexOf(ds) * c + p - 2)
            .height(h)
            .strokeStyle("#142f97")
            .event("point", function() this.parent.i(this.parent.mouse().x));        
    });
        
    var label = vis.add(pv.Label)
        .left(dates(begin(ds)))
        .top(function() data.indexOf(ds) * c + p)
        .textAlign("left")
        .textBaseline("bottom")
        .text(ds.label);
});
  
vis.add(pv.Rule)
    .left(function() this.parent.i())
    .anchor("top").add(pv.Label)
        .textAlign("center")
        .text(function() pv.Format.date("%d/%b/%y %H:%M:%S")(new Date(dates.invert(this.parent.i())*1000)));

Table Name,<%= data.first.class.columns.map { |c| c.name.titleize }.join(",") %>
<% data.each do |d| -%>
<%= d.class.table_name %>,<%=raw d.class.columns.map { |c| d.send(c.name).to_json }.join(",") %>
<% end -%>